package survivalblock.non_unfiction.access;

public interface ClientPlayerTickAccess {
    boolean non_unfiction$getPreviousTickValue();
    void non_unfiction$setPreviousTickValue(boolean newValue);
}
